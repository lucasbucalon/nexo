(function () {
  window.Components = window.Components || {};
  window.Components["tooltip-global"] = {
    init() {
      // Componente placeholder: lógica real de tooltip está em src/modules/utils.js
      // Mantemos init vazio para evitar erros ao chamar window.Components['tooltip-global'].init()
    },
  };
})();
